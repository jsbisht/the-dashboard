import React from 'react';
import Input from '../input-component/Input';

class Card extends React.Component {
    render() {
        return (
          <div className="card">
            <Input />
          </div>
        );
    }
}

export default Card;
